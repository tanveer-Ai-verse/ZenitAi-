# docker-compose.yml — Auto-generated by GitCraft AI
# Usage: docker-compose up --build
#
# ⚠️  Store secrets in a .env file (already in .gitignore).
#     docker-compose reads .env automatically.

version: '3.9'

services:
  data-intelligence-:
    build: .
    ports:
      - "8501:8501"
    environment:
      # These are read from .env file — NEVER hardcode here
      - GROQ_API_KEY=${GROQ_API_KEY}
      - NGROK_AUTH_TOKEN=${NGROK_AUTH_TOKEN}
      - DEBUG=False
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/"]
      interval: 30s
      timeout: 10s
      retries: 3
